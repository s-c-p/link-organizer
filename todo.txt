implement a curator in server itself

ux
	some way to group the links, in a sorted order;; or do I do this with a #tag--modName

db
	enable `computer` for info sake, completely independent of others

routing
	implement a link-cache page which catches and stores (for this session) the nsfw/snowden links that didn't open

implement config (ideally update at each logout or custom click)
	turn off/on familty mode
	number of entries on a page (pagination)
	db file
	import sessions' timestamps
	count store for index file

cache
	recent searches
	search results of last search

production
	libs like umbrella, pure, etc. be delivered by real CDN not me in production
